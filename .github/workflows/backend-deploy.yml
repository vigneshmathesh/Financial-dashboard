name: Backend Deploy
on:
  push:
    paths:
      - "backend/**"
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t backend-app ./backend
      # Push to ECR/Deploy via SSH can be added here
